%article
  .constrain-width
    %p.leader
      recently
      .toc
        - blog.articles[0...10].each do |article|
          %p
            %span.copy= article.date.strftime("%B %d, %Y")
            %span &rarr; &nbsp;
            = link_to article.title, article